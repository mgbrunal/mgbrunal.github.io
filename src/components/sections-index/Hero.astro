---
import ArrowUpRight from "../../assets/icons/ArrowUpRight.astro";
---

<div class="relative w-full min-h-[100dvh] flex flex-col items-start justify-center">
  <!-- Video background -->
  <video
    class="absolute inset-0 h-full w-full object-cover"
    autoplay
    muted
    loop
    playsinline
    preload="metadata"
  >
    <source src="/videos/hero.webm" type="video/webm" />
  </video>

  <!-- Overlay: gradient from left for text legibility -->
  <div class="absolute inset-0 bg-gradient-to-r from-black/90 via-black/60 to-black/20"></div>

  <!-- Content -->
  <div class="relative z-10 w-full xl:max-w-[60%] px-5 sm:px-20 py-20 flex flex-col items-start gap-8">
    <h1 data-reveal class="text-4xl sm:text-6xl font-bold text-white leading-tight tracking-tight">
      Desarrollo de software enfocado en resultados reales
    </h1>

    <p data-reveal data-reveal-delay="120" class="text-sm sm:text-lg font-light text-white/80 max-w-[52ch]">
      Creamos páginas web, automatizaciones y soluciones digitales que
      ayudan a negocios a captar oportunidades y operar de forma más
      eficiente.
    </p>

    <div
      data-reveal
      data-reveal-delay="240"
      class="flex flex-col sm:flex-row items-start justify-start gap-4"
    >
      <a
        href="/info"
        class="flex flex-row items-center gap-2 bg-secondary text-black border-2 border-black font-semibold uppercase px-5 py-4 transition-[background-color,transform] duration-200 ease-out-strong can-hover:hover:bg-secondary/60 active:scale-[0.97]"
        >Quienes somos?<div class="w-6 h-6">
          <ArrowUpRight color="#000" />
        </div></a
      >
      <a
        href="/contacto"
        class="flex flex-row items-center gap-2 bg-tertiary text-black border-2 border-black font-semibold uppercase px-5 py-4 transition-[background-color,transform] duration-200 ease-out-strong can-hover:hover:bg-tertiary/60 active:scale-[0.97]"
        >Agendar asesoría gratuita<div class="w-6 h-6">
          <ArrowUpRight color="#000" />
        </div>
      </a>
    </div>

    <p data-reveal data-reveal-delay="360" class="text-xs sm:text-sm text-white/50 tracking-wide uppercase">
      Soluciones prácticas, sin complejidad innecesaria
    </p>
  </div>
</div>
